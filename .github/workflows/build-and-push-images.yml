name: Build and Push Docker Images

on:
  push:
    branches:
      - main
      - master
      - dev-transcription-service
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
      - master
      - dev-transcription-service
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag pour les images (défaut: latest)'
        required: false
        default: 'latest'
        type: string

env:
  REGISTRY: ghcr.io
  TRANSCRIPTION_SERVICE_NAME: transcription-service
  AI_SERVICE_NAME: ai-service

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        service:
          - name: transcription-service
            dockerfile: Transcription-service/Dockerfile
            context: .
          - name: ai-service
            dockerfile: Dockerfile
            context: ai-service

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for ${{ matrix.service.name }}
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ matrix.service.name }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=${{ github.event.inputs.tag }},enable=${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag != '' }}
            type=sha,prefix={{branch}}-
            type=sha,prefix=pr-{{number}}-,enable=${{ github.event_name == 'pull_request' }}

      - name: Build and push ${{ matrix.service.name }}
        id: build
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.service.context }}
          file: ${{ matrix.service.dockerfile }}
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

      - name: Image digest for ${{ matrix.service.name }}
        run: |
          echo "${{ matrix.service.name }} image built successfully"

  summary:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.event_name != 'pull_request'
    steps:
      - name: Generate summary
        run: |
          echo "## Images Docker construites avec succès 🎉" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Images publiées :" >> $GITHUB_STEP_SUMMARY
          echo "- \`${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.TRANSCRIPTION_SERVICE_NAME }}:${{ github.event.inputs.tag || 'latest' }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- \`${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.AI_SERVICE_NAME }}:${{ github.event.inputs.tag || 'latest' }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Ces images peuvent maintenant être utilisées dans votre \`docker-compose.yml\` !" >> $GITHUB_STEP_SUMMARY

